
public class simulationLoop {
    private world world; 
    private worldPanel panel; 

    public void start(){ 
        //TODO: javaax.swing.timer that calls world.tick() then panel.repaint() 
        // on a fixed interval (e.g. every 50ms)
    }
}